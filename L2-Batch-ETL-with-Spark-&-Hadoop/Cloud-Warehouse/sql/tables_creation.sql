create
or replace table bev_phev_2023 (
    powertrain varchar,
    total_stock integer
);
create
or replace table sales_by_year (year date, total_sales float);
create
or replace table sales_by_region (region varchar, region_sales float);